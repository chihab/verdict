{
  "private": true,
  "name": "versem",
  "version": "0.1.3",
  "license": "MIT",
  "main": "./dist/index.js",
  "files": [
    "./dist/index.js"
  ],
  "bin": "dist/index.js",
  "scripts": {
    "watch": "yarn workspaces foreach run watch --parallel",
    "build": "yarn workspaces run build"
  },
  "workspaces": [
    "packages/**",
    "projects/**"
  ],
  "devDependencies": {
    "@types/node": "^14.14.37",
    "typescript": "^4.2.3"
  }
}
