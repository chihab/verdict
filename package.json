{
  "private": true,
  "name": "verdict",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "watch": "lerna run watch --parallel",
    "build": "lerna run build",
    "bundle": "lerna run bundle",
    "publish": "lerna publish"
  },
  "workspaces": [
    "packages/**",
    "projects/**"
  ],
  "devDependencies": {
    "@types/node": "~8.9.4",
    "typescript": "~3.4.3"
  },
  "dependencies": {
    "lerna": "^4.0.0",
    "@vercel/ncc": "^0.27.0"
  }
}
